import "package:flutter/material.dart";

class MidleFolderCard extends StatelessWidget {
  const MidleFolderCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}